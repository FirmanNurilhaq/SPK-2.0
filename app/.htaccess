<IfModule mod_rewrite.c>
	RewriteEngine On

	# TAMBAHKAN BARIS INI (Sesuaikan dengan nama folder anda)
	RewriteBase /spk-2.0/public/

	RewriteCond %{REQUEST_FILENAME} !-f
	RewriteCond %{REQUEST_FILENAME} !-d
	RewriteRule ^(.*)$ index.php/$1 [L]
</IfModule>